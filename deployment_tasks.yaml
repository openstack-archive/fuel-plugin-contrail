#
# Groups
# Contrail - DB
- id: primary-contrail-db
  type: group
  role: [primary-contrail-db]
  tasks: [hiera, globals, tools, logging, netconfig, hosts]
  required_for: [deploy_end]
  requires: [deploy_start]
  parameters:
    strategy:
      type: one_by_one
- id: contrail-db
  type: group
  role: [contrail-db]
  tasks: [hiera, globals, tools, logging, netconfig, hosts]
  required_for: [deploy_end]
  requires: [deploy_start, primary-contrail-db]
  parameters:
    strategy:
      type: parallel
#
# Contrail - Config
- id: primary-contrail-config
  type: group
  role: [primary-contrail-config]
  tasks: [hiera, globals, tools, logging, netconfig, hosts]
  required_for: [deploy_end]
  requires: [deploy_start]
  parameters:
    strategy:
      type: one_by_one
- id: contrail-config
  type: group
  role: [contrail-config]
  tasks: [hiera, globals, tools, logging, netconfig, hosts]
  required_for: [deploy_end]
  requires: [deploy_start, primary-contrail-config]
  parameters:
    strategy:
      type: parallel
#
# Contrail - Control
- id: primary-contrail-control
  type: group
  role: [primary-contrail-control]
  tasks: [hiera, globals, tools, logging, netconfig, hosts]
  required_for: [deploy_end]
  requires: [deploy_start]
  parameters:
    strategy:
      type: one_by_one
- id: contrail-control
  type: group
  role: [contrail-control]
  tasks: [hiera, globals, tools, logging, netconfig, hosts]
  required_for: [deploy_end]
  requires: [deploy_start, primary-contrail-control]
  parameters:
    strategy:
      type: parallel

#
# Create apt pin for Contrail repo
- id: contrail-repo
  type: puppet
  groups: [primary-controller, controller,
    cinder, cinder-vmware, compute, ceph-osd,
    primary-mongo, mongo,
    primary-contrail-db,contrail-db]
  required_for: [deploy_end, fuel_pkgs]
  requires: [deploy_start]
  parameters:
    puppet_manifest: puppet/manifests/contrail-repo.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 720
#
# Create overrides for Hiera
- id: controller-hiera
  type: puppet
  groups:  [primary-controller, controller]
  requires: [globals]
  required_for: [logging]
  parameters:
    puppet_manifest: puppet/manifests/controller-hiera.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 120
#
# Install Contrail utils, java
- id: contrail-utils
  type: puppet
  groups: [primary-contrail-db, contrail-db]
  required_for: [deploy_end]
  requires: [deploy_start, contrail-repo-pin]
  parameters:
    puppet_manifest: puppet/manifests/contrail-utils.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 720
#
# Install Cassandra and wait cluster to bootstrap
- id: contrail-db-seed
  type: puppet
  groups: [primary-contrail-db]
  required_for: [deploy_end]
  requires: [contrail-utils]
  parameters:
    puppet_manifest: puppet/manifests/contrail-db.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 3600

- id: contrail-db-bootstrap
  type: puppet
  groups: [contrail-db]
  required_for: [deploy_end]
  requires: [deploy_start, contrail-utils]
  parameters:
    puppet_manifest: puppet/manifests/contrail-db.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 3600
#
# Install and configure Contrail Conifg, Contrail Analytics, Contrail WebUI
# this task is performed on one node initially to allow discovery service to start up
- id: contrail-config-primary
  type: puppet
  groups: [primary-contrail-config]
  required_for: [deploy_end]
  requires: [contrail-db, contrail-utils]
  parameters:
    puppet_manifest: puppet/manifests/contrail-config.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 1440
#
# Perform same actions on other contrail-config nodes
- id: contrail-config-all
  type: puppet
  groups: [contrail-config]
  required_for: [deploy_end]
  requires: [contrail-config-primary, contrail-utils]
  parameters:
    puppet_manifest: puppet/manifests/contrail-config.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 1440
#
# Install and configure Contrail Control Nodes
- id: contrail-control-all
  type: puppet
  groups: [primary-contrail-control, contrail-control]
  required_for: [post_deployment_end]
  requires: [contrail-config-all, contrail-utils]
  parameters:
    puppet_manifest: puppet/manifests/contrail-control.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 1440
#
# Perform initial Contrail provisioning:
# provision BGP, encapsulation types and metadata services
- id: provision-contrail-control
  type: puppet
  groups: [primary-contrail-control]
  required_for: [post_deployment_end]
  requires: [contrail-control-all, contrail-utils]
  parameters:
    puppet_manifest: puppet/manifests/contrail-provision.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 1440
#
# Configuration for Neutron and Heat on OpenStack Controllers
- id: openstack-controller-configure
  type: puppet
  groups: [primary-controller, controller]
  required_for: [post_deployment_end]
  requires: [post_deployment_start]
  parameters:
    puppet_manifest: puppet/manifests/controller-config.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 720
#
# Create networks for ostf-tests and provision route target
- id: openstack-controller-provision
  type: puppet
  groups: [primary-controller]
  required_for: [post_deployment_end]
  requires: [post_deployment_start, openstack-controller-reconfigure]
  parameters:
    puppet_manifest: puppet/manifests/controller-provision.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 720
#
# Remove Open vSwitch packages, install and configure Contrail vRouter dkms kernel module
- id: compute-contrail-vrouter
  type: puppet
  groups: [compute]
  required_for: [post_deployment_end]
  requires: [post_deployment_start]
  parameters:
    puppet_manifest: puppet/manifests/site-compute-post.pp
    puppet_modules: puppet/modules:/etc/puppet/modules
    timeout: 720
#
# Reboot compute nodes, so they pick new network configuration
- id: compute-reboot
  type: shell
  groups: [compute]
  required_for: [post_deployment_end]
  requires: [compute-contrail-vrouter]
  parameters:
    cmd: if [ ! -f /opt/contrail/reboot-vrouter-DONE ]; then touch /opt/contrail/reboot-vrouter-DONE && /sbin/reboot; fi
    timeout: 720
